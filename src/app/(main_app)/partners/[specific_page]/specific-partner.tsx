export default async function SpecificPartnerComponent( { data }: { data: any } ) {
    console.log(data) // rag data response
    return (
        <>
            <h1>hi</h1>
        </>
    );
    
}